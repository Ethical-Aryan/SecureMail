that i have used to deleted his commits
Here are the exact Git commands used to delete Sumit's commits and restore your repository history:

Fetch Remote Changes: Fetched all remote commits from GitHub to inspect the remote branch status:

bash
git fetch --all
Inspect Commit History: Logged all local and remote branches to visualize the history graph:

bash
git log --all --graph --oneline
Reset Local HEAD: Pointed your local main branch HEAD to your latest local commit (ccef10d), skipping Sumit's commits:

bash
git reset --hard ccef10d
Force Push: Overwrote the remote branch on GitHub with your local clean commit history:

bash
git push origin main --force


# adding collaborators commit in ur code editor for eg vscode 

# Check the current status of your repository
git status

# Download the latest changes from GitHub
git pull origin main

# Stage (add) all modified, new, and deleted files
git add .

# Create a new commit with a meaningful message
git commit -m "Describe your changes"

# Upload your commit to the GitHub repository
git push origin main

# View commit history in a compact graph format
git log --oneline --graph --decorate

# Check the current branch
git branch

# Show all remote branches
git branch -r

# Show the configured remote repository URLs
git remote -v

# Check the status again to confirm everything is up to date
git status

# good for daily workflow 
# Get the latest code from GitHub
git pull origin main

# Add all your changes
git add .

# Save your changes as a commit
git commit -m "Your commit message"

# Push your changes to GitHub
git push origin main